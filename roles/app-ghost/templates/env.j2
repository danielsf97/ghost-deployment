DEBUG = False
TESTING = False
DB_URL = 'postgres://{{ app.db.user }}:{{ app.db.password }}@{{ hostvars[groups['database'][0]]['ansible_%s' | format(ansible_interfaces[0])].ipv4.address }}/{{ app.db.name }}'
INSERT = 'INSERT INTO passwords (password, password_hash) VALUES (:password, :password_hash)'
DELETE = 'DELETE FROM passwords WHERE id = :id'
SELECT = 'SELECT * FROM passwords'
